<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ pageTitle or siteData.name }}</title>
    <link href="/css/style.css" rel="stylesheet">
  </head>
  <body>
    {{ content | safe }}
    
    <h2>💌 All posts</h2>
    <ul>
      {%- for post in collections.posts -%}
        <li>{{ post.date.toLocaleDateString() }} - <a href="{{ post.url }}">{{ post.data.title }}</a></li>
      {%- endfor -%}
    </ul>
    
    <footer>
      Made by <a href="https://twitter.com/compysando">Tyler</a>, based on <a href="https://glitch.com/~geoglitchies">Geoglitchies</a>.
    </footer>
  </body>
</html>